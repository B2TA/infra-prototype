production: &default
  encryption_key: <%= ENV.fetch("ENCRYPTION_KEY") %>
  jwt_encryption_keys:
    - <%= ENV.fetch("JWT_ENCRYPTION_KEY", ENV.fetch("ENCRYPTION_KEY")) %>
  lti_iss: 'https://canvas.instructure.com'

development:
  <<: *default

test:
  <<: *default
